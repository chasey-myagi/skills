# Linus 独立静态校准

本报告是静态模型评估，没有运行代码、测试、子进程 reviewer 或外部服务。只依据指定 rubric 与 calibration-inputs.json 中 A/B/C/F 的代码和契约。下列行号按各案例变更后的代码片段从 1 起计，不冒充完整文件行号。评级只覆盖指定模式的审查对象。

## A — diff

**评级：Applied.；结论：通过，无阻塞 finding。**

这段代码平铺直叙：先处理缺省输入，再检查解析结果和范围。没有值得为了人设而制造的结构问题。新增行为可从代码确定：undefined、空白字符串、NaN、低于 1 或高于 65535 的解析结果返回 3000。

**本次引入或加重的问题：无可证明的问题。** `parseInt("80junk", 10)` 仍得到 80，但旧实现也是如此；输入没有要求严格的十进制整串匹配，不能把既有解析宽松性升级成本次阻塞。没有 spec 和测试属于证据限制，不自动等于行为错误，也不能据此要求 fail-fast。静态通过不表示已验证调用方集成。

**可选简化：无必要项。** 两个返回 3000 的分支分别保护字符串操作与处理解析结果，重复这个字面量不足以证明实际维护负担。

**Human Callouts：** `src/config.ts:1-8` 放宽入参类型，并让缺省或部分无效配置得到默认端口 3000；这是可见的公共契约和默认行为变化，需要知情，不构成独立缺陷。

## B — diff

**评级：Applied.；结论：本次变更通过，无阻塞 finding。**

字符串拼接换成模板字符串，SQL 内容和未处理输入进入 SQL 的方式均未改变。本次补丁没有消除安全问题，也没有引入或加重它。不能拿基线的债给本次变更开 NAK。

**本次引入或加重的问题：无。**

**基线信息：** `src/users.ts:2-3` 现有直接拼接方式允许输入改变 SQL，例如 `name = "' OR TRUE --"` 会改变 WHERE 条件；带单引号的正常姓名也会破坏语句。旧代码生成相同 SQL。这是明确的既有问题，单独记录，不进入本次修复队列，不影响本次评级。这里的通过仅针对 diff，不是对整个函数安全性的认可。

**可选简化：无必要项。** 单次使用的 `sql` 局部变量并不构成有证据的复杂性负担。

**Human Callouts：无。** 此次没有证据支持新增迁移、依赖、权限、公共契约或默认值变化。

## C — diff

**评级：Please fix and resend.；结论：NAK。**

这不是字符串写法调整。你把数据库参数重新塞回了 SQL 语法里，删掉的是一个真实的边界。

**C-01 — P1；blocking: true；`src/users.ts:2-3`：取消参数绑定，引入 SQL 注入并破坏精确查询。**

触发：将原始 HTTP 参数 `name = "' OR TRUE --"` 传入函数。期望：按契约只查询这个完整姓名，输入不改变 SQL 条件。实际：生成 `SELECT * FROM users WHERE name = '' OR TRUE --'`，查询条件可匹配其他用户。正常输入 `O'Reilly` 也会生成语法错误，而旧 `$1` 绑定能将它作为数据处理。影响路径就是 `GET /users?name=...` 到 `findUser` 的数据库调用；不需要假定额外权限或下游处理来证明契约破坏。

修复：恢复已知受支持的 `db.query("SELECT * FROM users WHERE name = $1", [name])`。不要再造一套手写转义规则。

**可选简化：无独立项。** 恢复参数绑定同时消除了拼装 `sql` 的需要。

**Human Callouts：无独立于上述缺陷的事项。** NAK 来自可定位的行为和安全退化，不来自知情提示。

## F — snapshot

**评级：Please fix and resend.；结论：NAK。**

这里评的是代码现状，没有 diff 归因豁免。当前函数没有实现它承诺的精确姓名查询。

**F-01 — P1；blocking: true；`src/users.ts:2-3`：原始姓名直接进入 SQL 语法。**

触发、期望与实际：输入 `"' OR TRUE --"` 应作为完整姓名匹配，实际生成恒真条件；输入 `O'Reilly` 应按姓名查询，实际破坏 SQL 字符串。受影响的是该函数的数据库查询路径。该缺陷由给定代码和契约直接支持，不需要猜测调用者权限。

修复：使用实际数据库驱动支持的参数绑定接口，将查询文本与参数分别传递。F 未给出驱动类型，不能声称已验证具体占位符语法；这只限制修复示例的精度，不影响缺陷成立。

**可选简化：无独立项。** 普通参数化查询足够，不需要新建查询框架。

**Human Callouts：无。**

## Rubric 一致性

未发现影响上述技术结论的实质矛盾。评级中的“有严重问题，需要大改”把严重程度与修改规模放在同一句，存在措辞歧义：C/F 是严重缺陷，但修复可能很小。本报告按严重程度选择 Please fix and resend，不借评级要求扩大改动。B 的基线问题只作信息记录；C 的新增问题与 F 的当前问题均进入 finding，归因与阻塞结论一致。
